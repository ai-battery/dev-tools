{
  "hooks": {
    "OnPluginInstall": [
      {
        "type": "command",
        "command": "${CLAUDE_PLUGIN_ROOT}/scripts/setup.sh",
        "description": "Install and verify required dependencies (prettier, typescript, eslint)"
      }
    ],
    "OnPluginUninstall": [
      {
        "type": "command",
        "command": "${CLAUDE_PLUGIN_ROOT}/scripts/uninstall.sh",
        "description": "Remove global npm packages and configuration files"
      }
    ],
    "PostToolUse": [
      {
        "matcher": "(Write|Edit|MultiEdit) && file_path =~ /\\.(ts|tsx|js|jsx|mts|cts|mjs|cjs)$/",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/scripts/quality-check.js",
            "description": "Run unified quality checks: TypeScript compilation, ESLint, Prettier, and React patterns"
          }
        ]
      }
    ]
  }
}
